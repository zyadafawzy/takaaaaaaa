# تِكّة — معرفة المشروع (Project Knowledge)

> مصدر الحقيقة الوحيد للأمر الأول. يُحدَّث في نهاية كل مرحلة.

## 1. الاسم والهوية

### الأسماء المقترحة (8) وتقييمها
| # | الاسم | النطق | الثقة | سهولة التذكر | ملاحظة |
|---|-------|-------|-------|--------------|--------|
| 1 | تِكّة | 5/5 | 4/5 | 5/5 | مصري، قصير، يربط بالوعد «في كام تكة» |
| 2 | سلّة | 5/5 | 4/5 | 4/5 | عام جدًا، صعب تمييزه |
| 3 | دكّانك | 4/5 | 4/5 | 4/5 | دافئ لكن يوحي بمحل صغير لا سوبرماركت |
| 4 | لمّة | 5/5 | 3/5 | 4/5 | عاطفي، أقل ارتباطًا بالتسوق |
| 5 | بيتنا ماركت | 4/5 | 4/5 | 3/5 | مكرر في السوق |
| 6 | هَنّا | 4/5 | 3/5 | 3/5 | غامض المعنى |
| 7 | زادنا | 4/5 | 5/5 | 3/5 | رصين لكنه فصيح أكثر من اللازم |
| 8 | كَرتونة | 5/5 | 2/5 | 4/5 | يوحي بالجملة لا بالبيت |

### الاسم المختار: **تِكّة**
السبب: مقطعان فقط، نطق مصري فوري بلا لبس، لا يشبه أي علامة قائمة، ويقفل الحلقة مع الوعد الأساسي.
الوعد: **«طلبات البيت اللي ناقصة، في كام تكة.»**

## 2. قرارات ثابتة
- لا حسابات للعملاء إطلاقًا: لا دخول، لا تسجيل، لا «حسابي»، لا «طلباتي». الدخول لفريق المتجر فقط عبر `/admin`.
- WhatsApp بدون API: نفتح رابط `wa.me` فقط. لا نقول أبدًا إن الرسالة أُرسلت. زر: «كمّل تأكيدك على واتساب». بعده: «طلبك اتسجل عندنا مبدئيًا — ابعت الرسالة على واتساب عشان نأكد معاك.»
- السلة محلية عبر `anonymous session id`، قابلة للمزامنة لاحقًا.
- الطلب يُنشأ عبر Adapter `createPendingOrder` (demo محليًا الآن، سحابي في الأمر الثاني).
- كل بيانات غير الحقيقية موسومة `demo` وتظهر بشارة «بيانات تجريبية».
- لا عبارات ضغط، لا مخزون وهمي، لا خصم مكتوب يدويًا (يُحسب من السعرين).
- الحماية الحقيقية للأدوار والصلاحيات تتم في قاعدة البيانات/الخادم في **الأمر الثاني**. إخفاء الأزرار في الواجهة ليس أمانًا.
- لا أسرار ولا API keys في الواجهة.

## 3. الهوية البصرية والـ Tokens
- أخضر طازج عميق (أساسي)، كريمي/أوف وايت (خلفيات)، برتقالي دافئ (حالات مهمة)، أحمر محسوب (تحذير)، وضع داكن كامل.
- كل الألوان `oklch` في `src/styles.css` كـ design tokens. ممنوع أي لون مباشر داخل المكوّنات.
- الخط: Cairo (fallback: IBM Plex Sans Arabic, system-ui).
- الأسعار بصيغة واحدة ثابتة: `125.00 ج.م` (أرقام لاتينية، اتجاه السعر لا ينقلب).
- الوضع الداكن/الفاتح يُحفظ محليًا ويحترم `prefers-color-scheme` أول زيارة.
- RTL حقيقي: `dir="rtl"` على المستند + منطق اتجاهي في التنقل والسلة والأسهم والـ Drawers.

## 4. الدومين (Types)
`Product`, `Variant`, `Category`, `Brand`, `Cart`, `CartLine`, `CheckoutDraft`, `Order`, `OrderStatus`, `AdminRole`, `DeliveryZone`, `Promotion`, `StoreSettings`, `SubstitutionPolicy`.

`OrderStatus`: `new | awaiting_whatsapp | needs_call | preparing | out_for_delivery | delivered | cancelled`.

`AdminRole`: `super_admin | store_manager | order_operator | inventory_operator | ceo_viewer` مع مصفوفة صلاحيات في `src/lib/auth/permissions.ts`.

## 5. المعمارية
```
src/
  routes/            صفحات العميل + /admin/*
  components/        مكوّنات صغيرة قابلة لإعادة الاستخدام
  domain/            types
  services/          repositories + adapters (demo الآن، cloud لاحقًا)
  lib/               pricing, formatting, arabic-normalize, whatsapp, storage
  data/              بيانات demo عربية قليلة وواقعية
```
لا منطق تسعير داخل المكوّنات المرئية.

## 6. المراحل واختبارات القبول

### م1 — الأساس
knowledge + الاسم + tokens + RTL + خط + PWA shell (manifest + SW) + تنقل سفلي + هوية بصرية.
قبول: RTL من 360px لـ desktop، Dark/Light يُحفظ، manifest صالح، SW مسجّل، لا نص إنجليزي للمستخدم.

### م2 — الكتالوج
Home + `/categories` + `/category/:slug` + `/search` + `/product/:slug` + demo repository.
قبول: بحث عربي مطبّع يعمل، بطاقة منتج بنقرة واحدة + Stepper، النافد لا يُضاف، لكل شاشة تحميل/فراغ/خطأ.

### م3 — الشراء
Cart + persistence + `/checkout` + `createPendingOrder` + `/order/pending/:token` + WhatsApp builder.
قبول: السلة تبقى بعد refresh، الملخص صحيح، زر واتساب لا يظهر قبل إنشاء الطلب، الرسالة عربية منظمة بلا ادعاء إرسال.

### م4 — الثقة
سياسات + `/track/:token` + 404 + accessibility + أداء وصور lazy.
قبول: تباين كافٍ، aria-live للسلة، keyboard navigation، احترام `prefers-reduced-motion`.

### م5 — الإدارة
`/admin/login` (mock adapter) + Guards + لوحة العمليات + مركز الطلبات.
قبول: مشغل mock يفتح طلبًا ويغيّر حالته عبر Action Adapter، الإلغاء يطلب سببًا، مؤشرات CEO موسومة Demo.

### م6 — التشغيل
الكتالوج + استيراد CSV/Excel بمطابقة أعمدة عربية + المخزون + العروض + مناطق التوصيل + الإعدادات + التقارير.
قبول: الاستيراد لا ينشر تلقائيًا، لا خصم وهمي، كل تقرير له تعريف وتاريخ تحديث وCSV.

### م7 — الجودة
اختبارات وحدات (الإجمالي، حدود الكمية، رسالة واتساب، التطبيع العربي، transitions) + checklist E2E + تدقيق RTL/responsive/dark/PWA + إصلاح الأخطاء.

## 7. خارج النطاق في الأمر الأول
قاعدة بيانات حقيقية، مصادقة حقيقية، RLS، OTP فعلي، مدفوعات، خرائط، WhatsApp API.
